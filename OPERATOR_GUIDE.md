# คู่มือเจ้าหน้าที่ — PAC Student Ticket 99 บาท

ระบบนี้เป็นระบบสิทธิ์นักศึกษาแยกจากระบบบัตรราคาปกติ ใช้สำหรับการตรวจสอบข้อมูลนักศึกษา การตรวจสลิป และการบันทึกการใช้สิทธิ์เท่านั้น

## 1. เข้าสู่ระบบ

1. เปิด URL ของ Web App นักศึกษาที่เจ้าของระบบสร้างขึ้นเอง
2. เข้าหน้า Admin ของแอปนี้ตามวิธีที่เจ้าของระบบกำหนด เช่น เพิ่ม `?page=admin` ท้าย URL
3. กรอก `ADMIN_TOKEN` ของ **ระบบนักศึกษา** และชื่อผู้ปฏิบัติงาน
4. ห้ามใช้ token ของระบบบัตรราคาปกติ และห้ามส่ง token ผ่านแชตหรือให้ลูกค้าเห็น

## 2. ตรวจรายการรอตรวจสอบ

ไปที่แท็บ `รอตรวจสอบ` ระบบจะแสดงรายการเก่าที่สุดก่อน เพื่อช่วยให้ตรวจตามลำดับเวลา ตรวจข้อมูลต่อไปนี้:

- Ticket ID
- ชื่อ-นามสกุล อีเมล เบอร์โทรศัพท์ รหัสนักศึกษา และรุ่น
- ยอดเงิน 99 บาท
- ภาพ `หลักฐาน RSU Connect` ต้องเห็นชื่อ-นามสกุล คณะ/สาขา และข้อมูลนักศึกษาอย่างชัดเจน
- `สลิปการชำระเงิน` ต้องอ่านได้และยอดตรงกับ 99 บาท

กด `ดู RSU Connect` หรือ `ดูสลิป` เพื่อเปิดภาพในหน้าต่างที่ได้รับอนุญาตแล้วเท่านั้น ห้ามดาวน์โหลดหรือส่งต่อไฟล์หลักฐานโดยไม่จำเป็น

## 3. อนุมัติ

กด `ตรวจรายการ` เพื่อดูข้อมูลและหลักฐานทั้งสองไฟล์ แล้วกด `อนุมัติสิทธิ์` เมื่อข้อมูลถูกต้อง สถานะจะเปลี่ยนเป็น `อนุมัติแล้ว` นักศึกษายังไม่ได้เลือกรอบการแสดงและยังไม่ได้ใช้สิทธิ์

การชำระเงินไม่ได้ทำให้อนุมัติอัตโนมัติ ต้องตรวจข้อมูลนักศึกษาและสลิปก่อนทุกครั้ง

## 4. ปฏิเสธ

หากหลักฐานหรือข้อมูลไม่ถูกต้อง ให้กด `ปฏิเสธรายการ` และบันทึกเหตุผล เช่น:

- ข้อมูล RSU Connect ไม่ชัดเจน
- ข้อมูลไม่ตรงกับที่กรอก
- รหัสนักศึกษาไม่อยู่ในช่วงที่กำหนด
- สลิปไม่ถูกต้อง
- ยอดเงินไม่ถูกต้อง
- อื่น ๆ

รายการที่ปฏิเสธจะไม่ถูกลบและยังอยู่ในประวัติถาวร นักศึกษาสามารถส่งรายการใหม่ได้ ระบบจะออก Student Ticket ID ใหม่ หากมีรายการ `รอตรวจสอบ`, `อนุมัติแล้ว`, หรือ `ใช้สิทธิ์แล้ว` อยู่ ระบบจะปฏิเสธการส่งซ้ำ แม้ใช้อีเมลเดิมกับรหัสนักศึกษาอื่น ระบบจะแจ้งเตือนเท่านั้น ไม่ได้ปฏิเสธอัตโนมัติ

## 5. ค้นหาและกรอง

แท็บ `รายการทั้งหมด` รองรับการค้นหาบางส่วนจาก:

- Student Ticket ID
- ชื่อ-นามสกุล
- อีเมล
- รหัสนักศึกษา
- เบอร์โทรศัพท์

กรองตามสถานะและรุ่นได้ และเรียงตามใหม่ที่สุด เก่าที่สุด ชื่อ หรือรหัสนักศึกษาได้

## 6. ส่งออก CSV

กด `ส่งออก CSV` หลังตั้งตัวกรองที่ต้องการ ไฟล์เป็น UTF-8 และมีข้อมูลสำหรับงานปฏิบัติการ เช่น Ticket ID ชื่อ อีเมล เบอร์โทรศัพท์ รหัสนักศึกษา รุ่น จำนวนเงิน สถานะ วันที่ตรวจ ผู้ตรวจ รอบที่ใช้สิทธิ์ วันที่ใช้สิทธิ์ และหมายเหตุ

ไฟล์ CSV ไม่รวม token, UUID ภายใน, หรือ Drive file ID

## 6.1 ค้นหาด้วยเบอร์โทรศัพท์

หน้า Admin ค้นหาได้ด้วย Student Ticket ID ชื่อ อีเมล รหัสนักศึกษา หรือเบอร์โทรศัพท์ ระบบจะตัดขีดและช่องว่างเพื่อค้นหาเบอร์ที่เก็บเป็นรูปแบบมาตรฐานเดียวกัน

หากมีเบอร์เดียวกันกับหลายรหัสนักศึกษา ระบบจะแสดงคำเตือน แต่ไม่ปฏิเสธรายการอัตโนมัติ เจ้าหน้าที่ควรตรวจรหัสนักศึกษาและหลักฐานให้ตรงกัน

หน้าลูกค้าตรวจสอบสถานะใช้รหัสนักศึกษาหรือเบอร์โทรศัพท์แบบตรงทั้งหมดเท่านั้น หากเบอร์เดียวกันอยู่กับหลายรหัสนักศึกษา ระบบจะไม่แสดงรายการและให้ตรวจสอบด้วยรหัสนักศึกษา

## 7. ใช้สิทธิ์หน้างาน

เมื่อผู้ถือสิทธิ์ที่มีสถานะ `อนุมัติแล้ว` มาถึงหน้างาน:

1. ค้นหาด้วย Ticket ID และตรวจชื่อ/รหัสนักศึกษา
2. กด `ใช้สิทธิ์`
3. ตรวจสอบอีกครั้งในหน้าต่างยืนยัน
4. หากทราบรอบที่เข้าชม ให้เลือกหนึ่งในสี่ตัวเลือก:
   - 21 Aug 2026 · 17:00
   - 21 Aug 2026 · 19:00
   - 22 Aug 2026 · 17:00
   - 22 Aug 2026 · 19:00
5. กด `ยืนยันใช้สิทธิ์`

การเลือกรอบนี้เป็นเพียงบันทึกการปฏิบัติงาน ไม่ได้แก้ไขความจุหรือข้อมูลของระบบบัตรราคาปกติ เมื่อสถานะเป็น `ใช้สิทธิ์แล้ว` จะใช้ซ้ำไม่ได้ หากระบบแจ้งว่าใช้ไปแล้ว ให้หยุดและตรวจสอบกับหัวหน้าหน้างาน ไม่กดซ้ำ

## 8. รายได้ที่ยืนยันแล้ว

หน้า `ภาพรวม` รวมรายได้จากยอด `amount` ที่เก็บไว้ของรายการสถานะ `อนุมัติแล้ว` และ `ใช้สิทธิ์แล้ว` เท่านั้น รายการรอตรวจสอบและปฏิเสธนับเป็นศูนย์
